name: sma2_frontend
description: Smart Meal Autopilot Flutter frontend
publish_to: 'none'
version: 0.1.0
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  provider: ^6.0.5
  shared_preferences: ^2.1.1
  android_alarm_manager_plus: ^5.0.0
  flutter_local_notifications: ^17.2.3
  android_intent_plus: ^5.0.0
  timezone : ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
